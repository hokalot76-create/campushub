<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title><%= typeof pageTitle !== 'undefined' ? pageTitle + ' | CampusHub' : 'CampusHub' %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <header class="top-nav">
    <div class="top-nav-left">
      <div class="logo-box">
        <span class="logo-dot">C</span>
        <span class="logo-text">CampusHub</span>
      </div>
    </div>
    <div class="top-nav-right">
      <% if (!currentUser) { %>
        <a href="/login" class="link-btn">Login</a>
        <a href="/student/signup" class="primary-btn small">Student Sign Up</a>
      <% } else { %>
        <span class="top-nav-user">Hi, <%= currentUser.name %> • <%= currentUser.role %></span>
        <a href="/logout" class="link-btn">Log out</a>
      <% } %>
    </div>
  </header>
  <main class="page-wrapper">
